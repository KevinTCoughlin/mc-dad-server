# Bun scripting sidecar configuration
# RCON_PASSWORD is passed via environment variable at runtime (not stored here)
RCON_PORT={{.RCONPort}}
RCON_HOST=127.0.0.1
MC_SERVER_DIR={{.ServerDir}}
# RCON_BLOCKED_COMMANDS=op,deop,stop,ban,ban-ip,pardon,pardon-ip,whitelist,save-off,save-all,save-on
# RCON_RATE_LIMIT=20
# RCON_RATE_BURST=40
# WEBHOOK_HOST=127.0.0.1
# WEBHOOK_PORT=9090
